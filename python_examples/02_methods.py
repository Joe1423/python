#MÉTODOS GENERALES 

variable = 'hola'

len() #longitud de una variable

variable[0] #devuelve lo que hay en primera posición

dir() #Devuelve todos los métodos aplicables al objeto que se pasa como argumento

help(variable.replace) #Para saber que hace un método en concreto se pueden pasar como argumento a la función help